<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">

    <title>Agenda de contatos</title>
      <style>
        /* Define uma fonte padrão para a página */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f5;
            margin: 0;
            padding-top: 60px; /* Ajuste conforme a altura do seu header */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh; /* Garante que o conteúdo seja visível */
        }

        .table-responsive {
            width: 100%;
            margin-top: 20px;
        }
        .fixed-table-container {
            position: fixed;
            top: 100px; /* Ajuste conforme a altura da navbar */
            left: 0;
            right: 0;
            z-index: 999; /* Para garantir que a tabela fique sobre outros elementos */
            background-color: white; /* Fundo para a tabela */
            padding: 10px; /* Espaçamento ao redor da tabela */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Opcional: Sombra para destacar a tabela */
            overflow: auto; /* Para permitir rolagem interna caso haja muitas colunas ou linhas */
            max-height: calc(100vh - 150px); /* Para limitar a altura da tabela se o conteúdo for grande */
        }
        /* Estilização do container da página */
        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        label, input {
            display: block;
            width: 100%;
        }

        input[type="text"] {
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        button {
            background-color: #007bff;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }

        button:hover {
            background-color: #0056b3;
        }

        /* Ajusta o layout em telas menores */
        @media (max-width: 600px) {
            form {
                padding: 15px;
            }

            button {
                padding: 8px 12px;
            }
        }
        .navbar {
          position: fixed;
          top: 0;
          width: 100%;
          z-index: 1000;
        }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <% if (user) { %>
              <li class="nav-item">
                <p class="nav-link">Bem-vindo!</p>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/contato/index">Contatos</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
              </li>
          <% } else { %>
              <li class="nav-item">
                <a class="nav-link" href="/login/index">Login</a>
              </li>
          <% } %>
        </ul>
        <% if (user) { %>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <a class="nav-link" href="/login/logout">Logout</a>
            </li>
          </ul>
        <% } %>
      </div>
    </nav>